rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /chat-media/{uidA}/{uidB}/{fileName} {
      allow read, write: if request.auth != null
        && (request.auth.uid == uidA || request.auth.uid == uidB);
    }
  }
}
